// One entry point for the generated Prisma client, so nothing else in the
// monorepo imports "@prisma/client" directly.

export type {
  Application,
  Company,
  CompanyFounder,
  CompanyInvestor,
  CompanySubmission,
  Department,
  Founder,
  Investor,
  Job,
  Office,
  Resume,
  User,
} from "@prisma/client";
export { Prisma, PrismaClient } from "@prisma/client";
